#include <stdio.h>

int main()
{
    int num, i = 1;
    long factorial = 1;

    printf("Enter a number: ");
    scanf("%d", &num);

    while(i <= num)
    {
        factorial = factorial * i;
        i++;
    }

    printf("Factorial of %d = %ld", num, factorial);

    return 0;
}
